import { Container } from "@mui/material"
import { DrawerComponent } from '../components'

export const Layout = () => {
  return (
    <Container maxWidth="xl">
      <DrawerComponent />
    </Container>
  )
}
